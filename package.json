{
  "name": "veille_techno_projet",
  "version": "1.0.0",
  "description": "",
  "main": "electron/main.js",
  "scripts": {
    "transcribe": "ts-node src/cli/transcribe.ts",
    "clean": "tsc --build --clean",
    "noemit": "npx tsc --noEmit",
    "build": "tsc || npm run build:ui",
    "build:ui": "esbuild src/ui/renderer.ts --bundle --outfile=src/ui/renderer.js --platform=browser --format=esm --minify",
    "build:renderer": "npx esbuild src/ui/renderer.ts --bundle --outfile=src/ui/renderer.js --platform=browser --format=esm",
    "electron": "npm run build && electron .",
    "test": "jest --runInBand",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.10.2",
    "electron": "^39.2.6",
    "esbuild": "^0.27.1",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@huggingface/hub": "^2.7.1",
    "@huggingface/transformers": "^3.8.1",
    "@xenova/transformers": "^2.17.2",
    "dotenv": "^17.2.3",
    "ffmpeg-static": "^5.3.0",
    "wav-decoder": "^1.3.0"
  }
}
